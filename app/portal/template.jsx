// Overrides the marketing-site page-enter animation. The portal is an app
// and should feel instant on navigation — no slide-in.
export default function Template({ children }) {
  return <>{children}</>;
}
